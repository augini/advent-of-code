# import required module
import sys
# append the path of the sibling directory
sys.path.append('..')
from utils.get_input import get_sample

def count_paths(input):
  return "here we are going now"

print(count_paths(get_sample("sample_input.txt")))